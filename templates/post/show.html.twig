{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
<div class="container py-5">
    <h1>{{ post.title }}</h1>

    <div class="row">
        <div class="col-12">
            {{ post.content }}
        </div>
    </div>
</div>

{% endblock %}